import {populateUserBookshelfList} from './get-bookshelf-list.js';

document.addEventListener("DOMContentLoaded", async () => {
        populateUserBookshelfList();
        // getBookshelves
                // makes fetch request to get all that user's bookshelves


        // populate user bookshelf
                // call getBookshelves to get books
                // loop through the bookshelves
                        // call appendBookshelfLi
                // makes Welcome message
                        // capitalize username

        // add event listener to the add-book button
                // show the form field

        // add an event listener to the form field
                // commits to the database
                // calls create li
                // hides form field


});
